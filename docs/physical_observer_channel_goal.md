# Physical Observer-Channel Prove-Or-Kill Goal

Construct a finite pointer register coupled by one sector-local action to a
binary relational matter observable and a finite environment. Prove the exact
reduced pointer channel and its distance from Harlow's ideal observer
dephasing rule. Price the same action with explicit entropy, localization,
duration, energy, circuit-complexity, and gravitational-backreaction ledgers;
do not identify a model record entropy with `S_Ob` or a declared mass envelope
with a derived stress tensor.

Apply two copies of that same channel to entangled and separable two-region
states matched in their local marginals, one-setting record, and declared
resource budgets. Continue toward ER=EPR only if the action itself defines a
connectivity observable and a nonzero entanglement-specific contrast survives
the matched control. Otherwise retain any exact observer-channel theorem and
issue a binding ER=EPR STOP.

## Completion Disposition

**Completed 2026-06-22: observer theorem RETAINED; ER=EPR STOPPED on this
action.**

The exact finite result and proof are in
`docs/physical_observer_channel_result.md`. The matched controls produce a
standard entanglement-witness gap but no derived connectivity quantity, so the
promotion condition is not met.
