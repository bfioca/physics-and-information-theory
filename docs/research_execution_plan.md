# Research Execution Plan

Status: Paper A specialist-review draft; long-term Paper U program

Branch context: inherited from `codex/observer-algebra-research-program`.
The bounded spacelike-replication manuscript audit on
`codex/spacelike-replication-manuscript` ended in **NOVELTY STOP** after an
exact reduction to established joint-measurement added noise.
The proposed standalone global `SO(3)` risk fallback is also stopped: Hayashi
already solves the exact Haar mean-Casimir optimization, including the
projective sector and sharp asymptotics.
The current optical common-mode formulation is likewise stopped as a
standalone paper after exact overlap with the Hu-Yu de Sitter spectrum and
standard decoherence-free-subspace physics. Boundary-regularity universality
is retained as a consequence, not a headline, because general endpoint
Fourier/Hankel asymptotics are established.

Last audited: 2026-06-12

## Verdict

This repository is a credible starting point for a paper-worthy mathematical-
physics program. It is not yet evidence of a gravitational-physics breakthrough.
The finite reference-frame, fuzzy-regulator, and local-QFT components are useful,
but they currently live in different controlled models.

The selected near-term paper is the computer-assisted Dirichlet-confined
Skyrmion profile theorem, strengthened by an analytic local parameter branch,
an authenticated radial Friedrichs gap, and a sharp intrinsic `p^-3` inertia-transform
tail. Its mandatory proof, exact-replay, package-audit, and internal referee
gates now pass after the reconstruction corrected the reference operator,
removed stale standalone AU.1 provenance and the underdefined bath corollary,
and made the closest prior art explicit. The class-uniform localization-reference-coherence-
backreaction inequality remains the primary long-term physics target, with the
Skyrmion as a realization rather than the universal headline.

The current paper-level finish line is the self-contained Paper A package in
`paper/validated_skyrmion_profile/`: manuscript, exact proof archives, clean
replay, package audit, and specialist referee pass.  The broader
`harlow_facing_paper_goal.md` remains a long-term Paper U specification for one
relational directional record, one common state/protocol family, one action
and approximation ledger, and a certified gravitational footprint.

The conceptual packet specified by `harlow_review_packet_codex_goal.md` is an
already-prepared, separate request for feedback on the observer-register
architecture.  It is neither the Paper A manuscript nor a gate for its
specialist review.

## Central Question

```text
Can a finite, localized, weakly gravitating observer physically realize enough
rotational reference information to recover the hard-energy angular sector of a
static patch, and if not, what quantitative obstruction survives the full local
matter dynamics?
```

## Publication Ladder

### Paper U: Class-Uniform Observer Tradeoff

Claim target: a model-independent or explicitly class-uniform lower bound on
orientation/recovery error, optical support, coherence time, or backreaction,
derived from named reference-quality, locality, energy, and gravitational
hypotheses.

The exact work packages and falsification gates are in
`universal_static_patch_observer_tradeoff_plan.md`.

### Paper A: Validated Profile (**selected manuscript**)

Claim target: certified base-point existence and local uniqueness for the
prescribed Dirichlet fixed-background Skyrmion profile, a qualitative locally
unique analytic branch in mass/curvature/wall radius, strict monotonicity,
negative wall slope, finite positive rotor inertia, a radial Friedrichs gap,
and an authenticated boundary-induced inertia-transform tail.

Not claimed: global uniqueness, Einstein-Skyrme backreaction, or a new de Sitter
observer algebra. The present branch theorem gives no explicit parameter-box
width, and the optical transform is not promoted to a bath factor.

### Paper B: Certified Matter Realization

Claim target: one declared total matter-membrane-bath model, action, state, and
control protocol supply the profile, current, switching, bath spectrum, ULE
remainder, preparation time, lifetime, stress, and weak-backreaction bounds on
an open parameter set.

### Paper C: Observer-Algebra Transfer

Claim target: the physical finite-time matter channel is close in normalized
diamond distance to the effective collective channel, so the all-decoder
finite-reference obstruction transfers with a controlled correction.

## Work Packages

| Gate | Deliverable | Pass condition | Kill or pivot condition |
| --- | --- | --- | --- |
| UO.1-UO.5 | Class-uniform observer inequality | One set of variables links reference quality, optical/proper support, coherence, energy, and backreaction over a declared observer class | The bound depends irreducibly on the chosen spherical top, hard wall, or append-and-twirl surrogate |
| AU.1 | Exact nonlinear profile certificate | Radii polynomial and contraction close; post-Newton sign and inertia checks pass | No admissible radius after joint norm-weight optimization and a targeted Hessian reformulation/refinement, or novelty overlap eliminates Paper A |
| AU.2-AU.3 | Certified spectral ledger | Conservative directed bounds for `Q0,Q1,Q2,G,M1` are complete; sharpen them enough to test whether the spectral/ULE upper constants are useful | Even sharp profile-resolving bounds make the ULE residual larger than the proposed signal after nonzero deadline, preparation, lifetime, and stress constraints are imposed |
| Action | One covariant worldtube model | Profile, Noether current, support, switch, stress tensor, and symmetry action follow from the same action | Boundary topology, support stress, or collective projection cannot be controlled |
| Channel | Physical-to-effective norm theorem | Explicit `eta_local` in normalized diamond distance, or a justified finite-dimensional upgrade | `eta_local` is as large as the recovery obstruction |
| Window | Open physical parameter set | Lifetime, localization, compactness, peak stress, junction residual, stability eigenvalue, and metric perturbation retain margins | Only isolated fine-tuned points exist, or no weak-backreaction branch exists |
| Consequence | Prediction or no-go | Observable shift exceeds the complete certified uncertainty, or a live observer dictionary is excluded under explicit assumptions | Result reduces to an existing QRF/ULE theorem after substituting model constants |

## Paper A Release Disposition

1. **Closed:** the raw-spline Jacobi norm, Friedrichs radial realization,
   source-consistent combined archive, and removal of the bath corollary.
2. **Closed:** clean exact replays of the combined AU.1/AU.2 certificate,
   sharp-tube snapshot, and radial-gap certificate.
3. **Closed:** rebuilt and visually inspected PDF, refreshed manifest, focused
   proof tests, package tests, and mathematical/publication referee audits.
4. **Deferred enhancement:** an explicit rational `(mu^2, lambda)` rectangle
   at fixed wall radius. The qualitative analytic branch is valid without a
   numerical width, and the manuscript says so explicitly.
5. **Next:** obtain external human priority/correctness review and add ordinary
   affiliation, contact, immutable-tag, or DOI metadata for submission.
6. **Then:** resume Paper U. The optical route may be reactivated only after a
   uniform six-channel KMS reduced-channel estimate; the boundary route
   requires an action-derived topology-preserving crossover family rather than
   another integration-by-parts theorem.

## Current Exact Status

- Canonical fuzzy-sphere refinement and fixed-domain convergence: complete as a
  component theorem.
- Finite compact-reference recovery and obstruction bounds: complete within the
  declared effective channels.
- Quintic origin branch identification and endpoint-corrected nonlinear forcing:
  complete.
- Green-resolvent audit: defect `<=0.08901403484568955`, with
  `C0<=0.22813715684064623`, `C1<=1.4321231091914504`, and
  `C2<=59.31251557489701`.
- Fixed-wall radial dynamical gap: complete for the exact AU.1 solution.
  Authenticated adaptive Newton-tube replay and a cancellation-preserving
  origin audit prove `L_Jacobi>=1` on the physical `l=0` form domain. Since
  `W<=25`, `omega_hat_rad>=1/5`; certificate SHA256
  `695310609d070f6dba2c678982ffc87472ed7c341813137fb90ad9dd9e1e6096`.
  The moving membrane is handled by the next item; anchor and nonspherical
  complement channels remain open.
- Coupled spherical profile-membrane radial gap: complete. A second rational
  Barta witness passes both the authenticated exact-profile bulk test and the
  moving-wall boundary test. Exact Young-Laplace cancellation gives
  `M_0=800/47`, boundary form coefficient `39878/69325`, and the unconditional
  floor `omega_hat_l0>=1/50`; certificate SHA256
  `2bb48f770504ab5a0a0f9b3139b881877a414ddb9b8c19cd34c81bfd26b42686`.
  The branch-coordinate `0.198` value remains an optional floating sharpening,
  not a premise of the positive theorem.
- AU.1 nonlinear profile certificate: complete for the prescribed
  `mu^2=1`, `lambda=1/400`, Dirichlet boundary `x=4` problem. With `omega=3/4`,
  `r=1/250`, 12-term rigorous trigonometric enclosures, and outward rounding to
  denominator `10^18`, the exact audit proves `Y<=0.002296`,
  `Z0<=0.000078`, `Z2<=78.733`, `p(r)<-0.00107`, and contraction
  `q(r)<0.31501`. The unique solution in the Newton ball is strictly decreasing,
  its wall slope lies inside `[-0.09465,-0.08746]`, and its dimensionless rotor
  inertia lies inside `[21.149,48.921]`. Invertibility of the exact-solution
  derivative gives a qualitative locally unique analytic branch in
  `(mu^2,lambda,x_w)`; no explicit box width is yet certified. The canonical
  AU.1 data are carried by the source-consistent combined AU.1/AU.2 archive,
  not the historical standalone extraction.
- AU.2 global derivative and continuum-tail certificate: complete. Exact
  positive-radius 43-cell sums plus the regular-origin Volterra-Lie interval-AD
  proof give
  `M^W<=(2.5776861728e8,6.9465700567e6,2.9826900811e5)` and
  `M^A<=(3.7317164259e10,5.0564413001e8,1.3919447366e7)`. The spectral ledger
  contains all six inputs and a non-null exact-rational tail envelope. Archive
  SHA256: `1d5fe53786cc280006d7b1092d360556d4d8d8684e5ae3356ce8cd6d084e72a9`.
- AU.3a global Sobolev certificate: complete conservatively. A normalized
  positive-inertia-measure kernel theorem, 512 exact rational frequency cells
  on `0<=p<=128`, and the AU.2 tail recomputed at the same join give
  `Q<=(4296.791,10146.946,35213.763)`, `G<=16554.539`, and
  `M1<=47391.581`. Archive SHA256:
  `b6a9931cc50359ec4a9bed7a6d3443471f39b39f60c37e3a92d39073ba0cc55c`.
  These close global finiteness and provenance, not sharpness.
- AU.3b authenticated baseline: complete for `P=64`, unit frequency cells, one
  positive-radius subdivision, two origin cells, and deterministic 16-worker
  reduction. It gives
  `Q<=(17526.909,53893.637,242992.971)` under SHA256
  `bc6cf2ea21f44c122001fcc3f7fa6cffb9983d4ec4e35591323aa2d29b25c529`.
  The result is rigorous but weaker than AU.3a: the conservative AU.2 tail
  dominates the finite-band squared bounds by about `614`, `543`, and `137`.
  This diagnoses the current tail/product enclosure, not a physical no-go.
- Global orientation-resource theorem: complete for a Haar-prior `SO(3)`
  chordal task. Every state and POVM obeys the direct fusion/Hardy inequality
  `R_ref>=1/(16<J^2>+8)` and the independent information inequality
  `R_ref>=6/(e*pi^(5/3))*exp[-2A_SO3/3]`; a Gibbs variational bound converts
  mean spin into a tail-robust inverse-square risk floor. Combining either
  route with the existing spherical-top compactness budget gives an explicit
  all-state `G^2/a^4` localization-risk corollary. The extension from that
  constitutive model to a named local matter/stress class remains open.
- Conditional coherence theorem: complete for reference-only isotropic `SO(3)`
  heat diffusion. The spin-1 score attenuates exactly by `e^(-2 gamma tau)`,
  yielding a closed risk interpolation and necessary coherence-time ceiling.
  Deriving `gamma` and the Markov error from the same Skyrmion/KMS action is
  still open.
- Conditional elimination theorem: complete for the composite spherical-top,
  hard-current optical-support, and reference-heat branch. It checks a proper
  radius interval and an exact coherence ceiling for each design. Closed
  windows are rigorous branch no-gos; open windows remain necessary-only until
  one action supplies all inputs.
- Estimation-to-recovery bridge: complete for the spin-1 measure-and-correct
  protocol, with `(8/9)R_ref<=epsilon_rec<=2sqrt(R_ref)`. The remaining bridge
  is to derive this POVM/correction locally and to treat arbitrary coherent or
  higher-spin recovery without conflating it with estimation.
- Fermionic realization bridge: complete at the kinematic/fixed-profile level.
  The fusion theorem has been extended to the half-integer
  Finkelstein-Rubinstein sector, and the existing joint compactness/slow-rotation
  cutoff previously yielded a global Skyrmion risk floor (`8.0566e-5` for its
  coarse default audit). Authenticated directed substitution now gives
  interior inertia `[21.149280,48.390986]`, total mass
  `[34.210839,65.274327]`, `J<=352`, and the weaker conditional floor
  `R_ref>=1.9689304688982673e-5`. The explicit observer-capacity corollary tracks the same profile's
  areal, proper, and optical support and proves that the `e`/`f_pi` co-scaling
  cannot remove this floor at fixed budgets. Isospin access, local POVM
  implementation, KMS diffusion, stress, and lifetime remain open. The total
  mass includes the ideal Young-Laplace shell; the inertia is interior only and
  omits wall inertia.
- Spherical gravity bridge: complete for the radial Hamiltonian constraint of
  the authenticated field held fixed. The exact local theorem uses
  `q=2Gm/[rN]`, disproves endpoint compactness as a uniform criterion, and the
  directed profile replay gives `H_bulk<=29.246335626859388` with its maximum
  internal. This is not yet a lapse, self-gravitating field, membrane-junction,
  or rotating Einstein-matter theorem.
- Collective gravity-risk bridge: complete conditionally in the fixed-field
  leading-rotor sector. The radial metric budget bounds static mass below and
  inertia above; AM-GM eliminates `e` and the authenticated default gives
  `<J^2><=1.6476538e7` and `R_ref>=3.79327245124592e-9`. This removes the
  separate compactness proxy from that chain. Static lapse and the leading
  nonspherical source are now bounded below; their metric-response and
  collective-projection extensions remain separate gates.
- Static lapse bridge: complete for the fixed nonrotating bulk. The enthalpy
  identity cancels the radial metric factor from the lapse equation and the
  authenticated coefficient is `D<=43.445333`. At `beta=1/2` the minimum
  `g_tt` ratio diagnostic is only `0.1131949426`, so this point is controlled
  but does not meet a negligible-backreaction interpretation.
- Leading rotational stress multipole: complete at quadratic collective order.
  For `S=C delta/3+Q`, positivity gives sharp all-state bounds on `Q`, and the
  integrated absolute `l=2` rotational-energy source is at most
  `E_rot/sqrt(5)`. A pure spin-2 second-order-anticoherent state proves that
  `Q=0` is compatible with full-rank local rotational QFI, so an inevitable
  quadrupole no-go is false. The `Q=0` and bounded-all-state branches now have
  distinct gravitational completion gates.
- Global stabilizer ambiguity: complete for every exactly stabilized state.
  A stabilizer rotation of angle `alpha` forces
  `R_ref>=sin^2(alpha/4)`. The explicit spin-2 `Q=0` state has a half-turn
  stabilizer and hence `R_ref>=1/2`, so it cannot realize a precise global
  frame despite its full-rank local QFI.
- Fixed-background quadrupole response: complete for the conserved pure-de
  Sitter Zerilli master source. The exact positive Green kernel gives
  `||Psi||_2<=(R^2/6)||F||_2` and the near-horizon susceptibility
  `G_2(r,r)=R log(2R/rho)-3R/2+o(R)`.
- Response-input obstruction: complete. Gauge-dependent metric norms fail at
  zero source, and conserved spatial stress can have zero energy density but
  nonzero curvature. The existing rotational energy-multipole bound therefore
  cannot be composed until the full pressure/shear/membrane source is derived.
- Static stress-conservation gate: complete. The exact bulk radial and angular
  identities imply `p_perp=2pi` for an `ell=2`, `j=0` source, and hard
  truncation exposes the radial and angular wall-traction delta coefficients.
- Fixed-profile rigid-source audit: complete as a no-go for that approximation.
  The direct quadratic Skyrmion quadrupole has `j=0` but violates
  `p_perp=2pi`, with a strict nonzero-slope hard-wall asymptotic. It is not an
  admissible static Zerilli source without centrifugal material response.
- Centrifugal deformation gate: complete at the local variational and wall
  level. The quadrupole is a coupled rank-two `(f,g)` system, the scalar source
  exactly reproduces the radial conservation defect, the tangential source is
  generically nonzero, and a moving pure-tension mirror supplies an exact
  Robin condition. A validated global solve and membrane completion remain open.
- Exploratory global centrifugal branch: complete numerically at the default
  point. The regular indicial modes and moving-wall BVP give
  `max|f|=0.3054`, `max|g|=0.2736`, and `xi2=-0.28735`, with mesh,
  origin-cutoff, and profile-step convergence. This is not validated numerics.
- Same-action smooth-bulk stress closure: complete as floating evidence at the
  default point. The rigid-plus-deformation stress passes both exact static
  conservation identities with second-order mesh decay; at 801 nodes the
  combined residual is `7.55e-4` of the stable rigid defect.
- Distributional membrane completion: complete at fixed-background first
  order. The moving Nambu-Goto shell divergence factorizes exactly into
  curvature forces; the displaced bulk layer then reduces conservation to the
  BVP's Young-Laplace and traction laws. All four meshes close below `1.19e-12`.
  The declared weak form is now coercive. The exact Israel gate additionally
  proves that identical de Sitter geometry on both sides cannot support the
  nonzero tension and supplies a self-consistent de Sitter/Kottler benchmark.
  An independent membrane second variation, spherical Einstein-Skyrme/Kottler
  junction, and six-amplitude nonspherical reconstruction remain open.
- Coupled validation formulation: complete at the operator level and floating
  at the response level.
  Direct weak-form assembly of the same-action Hessian restores exact matrix
  symmetry, includes the Robin-equivalent wall term, gives nested lowest Ritz
  values approaching `0.35348`, and agrees with the separately implemented strong-form
  solution to `1.32e-4`. Authenticated inner/outer Liouville certificates and
  the weighted-form theorem now give `A>=(1/100)I` and `||A^-1||<=100`.
  The exact dual-weighted theorem and favorable floating audit reduce the
  exterior response to a residual product. Generic-scalar affine stress,
  smooth bulk-source, and center-regular six-jet kernels are complete; the
  moving-wall kernel is complete with exact endpoint cancellation. The
  positive-radius rational-trial residual and energy-dual integration theorem
  is complete. The cancellation-safe origin residual and exact weighted norm
  are also complete. Exact physical trial archives and their matched regular-
  origin polynomial patches are complete. Authenticated origin profile jets,
  the primal origin residual, and the primal wall residual are complete;
  exact conormal interface cancellation and the interval adjoint wall load are
  complete; the positive-radius adjoint bulk weak load is also certified.
  A centered outer coefficient enclosure now improves the primal residual
  square by more than `30000`, but its conservative dual lift is still too
  broad. The adjoint direct energy-dual lift is structurally complete but its
  positive-radius-plus-wall upper bound `0.7695` is also too broad.
  Newton-remainder correlation in both residuals and the regular-origin
  adjoint load remain open.
- Coupled-origin indicial theorem: complete exactly. Rational Laurent algebra
  proves `det K(p)=det(P0)(p-1)(p-3)(p+2)(p+4)`, with `P0` positive for every
  real origin slope and linear regular mode `f=-g`. The authenticated slope
  interval now also encloses the leading Robin matrix at `x=1/16`; its
  finite-cutoff Frobenius remainder, nonsingular transformed transfer, and
  Friedrichs-domain theorem remain open.
- Transformed-origin density theorem: complete exactly. The regular variables
  factor the full local static Hessian as `H_static=x^2 H_hat(t)`, with no
  negative powers of `t` and exact center value `H_hat(0)=v(0)^2/6`. The
  leading rotational source lies in `range K(3)`, so the forced cubic start is
  log-free and obeys an exact initial compatibility relation. The
  transformed action still carries the weight `sqrt(t)/2`; its weighted
  Euler-Lagrange trace theorem and interval transfer to `t=1/256` remain open.
- First post-indicial recurrence: complete exactly. Rational-function algebra
  fixes both homogeneous germs and the forced particular germ through `x^5`;
  `det M5=-(28/75)(32b^4+12b^2+1)<0` for every real slope. The full leading
  equation corrects the linear mode by a generally nonzero `v_t(0)`. A
  correlated Taylor remainder on the full origin cell remains open.
- Post-germ Green majorant: complete exactly. Across the authenticated slope
  box and every odd `p>=7`, `||K(p)^-1||_infinity<79/1000`; the direct `p=7`
  bound is the maximum.
- Uniform quintic origin family: complete exactly. Sixteen contiguous rational
  slope cells validate a common `t^3` profile remainder through `t=1/256`,
  with contraction below `3/5` and radii left side below `9/10`. The remaining
  origin task is now the conormal variable-coefficient/source bound and affine
  field-transfer radii inequality; the coarse cubic profile remainder is no
  longer the blocker.
- Conormal transfer scaffold: complete exactly. The first-order system and
  source blocks are derived without profile-remainder differentiation,
  `spec A0={0,2,-3,-5}`, and the weighted degree-three Green majorant is below
  `9/20`.
- Regular conormal blocks and formal residual: complete exactly. The full
  physical Hessian and rotational source have regular kernel-level blocks,
  `A-A0=O(t)`, `q=O(t)`, and the Euler-to-conormal identity proves `t^3`
  residual divisibility for both homogeneous columns and the forced column.
  The functional Taylor remainder is now closed as well.
- Finite-cell conormal transfer: complete by exact rational interval
  arithmetic. The authenticated two-cell family has `gamma delta<0.072576`,
  all three affine-column radii inequalities close, and the largest cutoff
  conormal-state component error is below `7.73e-5`.
- Physical origin transfer: complete by exact rational interval arithmetic.
  Direct `(f,g,f',g')` tubes now cover both authenticated slope cells and all
  three affine columns, with field width below `0.051139` and derivative width
  below `1.394047`.
- Local finite-energy trace: complete for solution germs. Exact leading-energy
  polynomials admit only `p=1,3` and exclude `p=-2,-4`; the two-dimensional
  homogeneous trace and forced cubic column are exactly the columns exported
  by the physical transfer. Extending this from solution germs to the closed
  global form and proving semiboundedness remain part of the global inverse.
- Exact conserved stress-to-master map: complete in a frozen
  Zerilli-Moncrief convention, including general thin-shell distribution jets
  and the contact-free off-wall representation.
- Completed Skyrmion master response: complete as source-hashed floating
  evidence at the default point. `psi0` is nonzero and mesh/origin/profile
  stable at five off-wall radii. Its ideal-shell contact and off-wall field/flux
  jumps now satisfy an exact transmission theorem.
- Physical collective normalization: complete at leading rigid-rotor and
  linear-gravity order. The exact conversion is
  `Psi_phys=(8piG e^3 f_pi/c_I^2)psi0 QJ_ab n_a n_b`, with `c_I` taken from the
  same profile and the anisotropic/anticoherent state split explicit. Interval
  validation and Israel matching remain.
- Exterior invariant reconstruction: complete in fixed-background linear
  gravity. The exact vacuum inverse map gives
  `delta E_rr=-6Psi Y/r^3`; the completed response is one horizon-regular
  exterior mode and produces a nonzero physical tidal quadrupole for the
  anisotropic branch.
- Detector composition: complete conditionally in the Jacobi, semiclassical,
  constant-rate heat, and Gaussian-readout model. In the instantaneous
  mean-field limit, equal-Casimir, equal-leading-rotor-energy spin-2 states
  give nonzero versus zero radial gradiometer signals; the exact finite-time
  rank-two kernel maps an input Weyl contrast to displacement SNR and error.
  Common-action rate/noise derivation, finite separation, non-Gaussian noise,
  fluctuations, backreaction, and self-consistent geometry remain.
- Matter-derived coherence rate: composed under the declared pseudoscalar
  Davies convention, `gamma_prop=g^2 N^-3 j_Sky(0)`. The executable now turns
  the certified projective risk floor into a proper-time coherence ceiling and
  tracks the loaded Markov diagnostic. AU.3a now certifies conservative global
  spectral moment inputs, but the exact finite-coupling reduced-dynamics,
  switching, band-projection, and access errors remain open before this becomes
  a physical theorem.
- Class-uniform observer composition: complete conditionally. Every declared
  capacity/localization model and isotropic heat exposure now enters the exact
  risk bound
  `3/4(1-e^-2Gamma)+e^-2Gamma/(16C_max+8)`, with an exact confined-orbital
  compactness corollary. Casimir remains an upper capacity budget, not a
  sufficient quality measure. The one-action optical, exposure, recovery, and
  relativistic metric/stress bridges remain incomplete.
- Physical local-channel diamond estimate, open stress/backreaction window, and
  external novelty review: incomplete.

## Active Goal Completion Audit

| Objective requirement | Current evidence | Completion verdict |
| --- | --- | --- |
| Physically derived static-patch/horizon observer algebra | Lorentzian Bunch-Davies local net, conditional finite-reference algebra, exact global orientation risk, and composite elimination theorem | Incomplete: the local measurement, reference, bath, switching, and gravitational budgets do not yet follow from one action |
| Canonical regulator and refinement maps | Fuzzy-sphere Berezin maps and fixed-domain convergence are proved as component theorems | Component complete, physical uniqueness incomplete: the matter observer has not selected this regulator from its operational algebra |
| Quantitative screen-data completeness or obstruction | All-decoder finite-reference and common-mode locality obstructions are exact in their declared channels | Incomplete physically: a normalized-diamond local matter-to-effective channel estimate is missing |
| Rigorous continuum convergence | All-angular optical/Bunch-Davies and Hadamard local-net limits are proved; AU.1-AU.3b certify authenticated fixed-profile spectral bounds | Incomplete jointly: the AU.3b baseline is tail dominated, and no controlled Einstein-matter/worldtube limit carries all observer inputs and error bounds |
| Novel observable prediction or no-go | Curvature-enhanced Skyrmion zero mode, optical common-mode obstruction, density-only gravity no-go, logarithmic near-horizon susceptibility, and an equal-leading-energy tidal reference discriminator are explicit candidates | Not paper-final: the tidal result is a floating semiclassical mean-field/Jacobi-limit prediction and none yet has a complete one-action uncertainty ledger on an open physical parameter set |
| Submission-ready central theorem | Exact conditional class-uniform tradeoff and orbital compactness/noise corollary, dependency ledger, authenticated AU.3b, supported-Skyrmion floor, Feshbach transfer, radial gaps, spherical metric/lapse response, authenticated global centrifugal inverse and nonzero weak matter response, exact stress-to-master reduction, floating exterior response, fixed-de Sitter `l=2` resolvent, exact tensorial Israel promotion gate, and exact conditional finite-time/noisy detector transfer exist | Not achieved: one-action nonzero exposure and optical map, interval exterior response, spherical Einstein-Skyrme/Kottler matching, six-amplitude nonspherical Israel reconstruction, deformed-background/higher-order gravity, common-action detector rate/noise/backreaction, quantum collective projection, anchor/nonspherical gaps, useful tail constants, finite-coupling dynamics, access, stress/lifetime control, and specialist review remain |

The transformed-origin majorant, finite-cutoff affine transfer, local
finite-energy solution trace, authenticated inner/outer nonlinear-family
Liouville audits, and global weighted-form theorem are complete. The declared
fixed-background operator is positive self-adjoint with
`||A^-1||_(L2->L2)<=100`. The next central task is an interval
forced-response calculation. The matter load has an `L2` representative and
already gives a unique nonzero weak deformation with positive conjugate
susceptibility. The off-wall Green kernel has now been reduced to one exterior
amplitude with the dual-weighted error `delta_y delta_z`; exact conforming
primal-adjoint trial splines are now archived, and the remaining work is to
certify tight full-domain residuals and make that amplitude interval exclude
zero. A source-hashed floating `41 -> 81` audit gives product error
only `1.60` percent of the corrected amplitude, so the interval target has a
substantial feasibility margin.
The observable algebra no longer relies on sampled differentiation: exact
affine bulk coefficients and the origin factorization
`F_master=x F_hat(x^2)` are implemented for rational, interval, and Taylor
arithmetic. Positive-radius exact rational `C1` trials now have a validated
cellwise residual and wall-to-energy-dual composition, and regular-origin
trials have a cancellation-safe `x^3/3` weighted residual theorem. The
physical primal/adjoint splines and matched origin patches are now archived.
The remaining response work is to sharpen the successful centered model by
preserving the generating Newton graph/slope correlation, tighten the direct
adjoint `V*` lift around its dominant `[1/2,67/128]` cell, add its origin load,
and evaluate the composed response
interval using the exact interface cancellation and certified primal/adjoint
wall data.
The Israel acceptance theorem now identifies the next gravitational inputs
without ambiguity: first solve the spherical Einstein-Skyrme/Kottler junction,
then reconstruct and certify the three first-form and three mixed-extrinsic-
curvature amplitudes on the displaced shell. The conditional finite-time noisy
detector transfer is now closed; deriving its Weyl interval, rate, noise, and
backreaction from the common action remains open. In parallel, the
`Q=0` branch now requires a globally accurate asymptotically free state family;
the explicit finite-spin example is excluded by its stabilizer. Collective
projection and higher-order rotation remain open. The anchor/nonspherical
Hessian, off-band coupling certificate, and finite-coupling
physical channel remain parallel gates. A sharper AU.3b tail/product enclosure
should be pursued only where it changes that joint error budget. Weak spectral
constants are not by themselves a physical no-go: any
obstruction must also impose nonzero timing, stress, lifetime, and gravity
constraints and separate interval overestimation.

## Reproducibility Rule

Every positive claim must name its model, norm, parameter domain, exact artifact,
and failure threshold. Numerical evidence without an outward-rounded certificate
may guide refinement but may not appear as a theorem premise.
