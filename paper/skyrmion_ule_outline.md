# Matter-Derived Static-Patch Noise From A Finite Skyrmion Worldtube

Status: realization/fallback manuscript outline; not the primary universal claim.

The primary hierarchy is now documented in
`universal_observer_tradeoff_outline.md`: class-uniform observer tradeoff,
Skyrmion realization, then exact spectral and ULE constants. This outline
remains independently useful if the universal quantum-capacity bridge does not
close.

## Target Claim

For one explicitly supported centered massive-Skyrmion worldtube in the static
patch, derive the collective current's optical bath spectrum and prove a fully
numerical, ancilla-stable finite-preparation ULE error bound with rigorous global
constants. The publishable result is the conjunction of a matter-derived form
factor, its curvature prediction and sharp wall tail, the signed-factor ULE
extension, and a certified nonempty protocol window.

This is a conditional mathematical-physics paper unless the worldtube action,
collective-band projection, stress, lifetime, and switching profile are all
closed in the same model. It is not a claim of a gravitational observer algebra
or a universal Markov theorem.

## Existing Theorem Chain

1. The centered collective current fixes `H_Sky(p)` and predicts a positive
   curvature enhancement of the zero-frequency rate.
2. A simple form-factor zero obstructs the principal-root `H2` proof route.
3. The scalar real signed factor `q=sqrt(j0)H_Sky` preserves the spectrum and
   zero-Bohr generator while removing the cusp.
4. A boundary-aware continuum theorem proves the sharp `p^-5` form-factor tail
   and global signed-factor `H2` membership.
5. A prescribed finite amplitude ramp and plateau burn-in add an explicit
   logarithmic history term without changing the two coupling exponents.
6. Pure optical translation cannot split the auto Kossakowski tensor; such a
   splitting is a diagnostic of intrinsic supported-source deformation.
7. Exact rational interval infrastructure is implemented for the validated
   profile checker.
8. A conditional exact-rational positive-radius Picard cell validates the
   nonlinear checker logic, but is not connected to the regular origin branch.
9. One weighted Volterra contraction validates the regular cubic origin family
   uniformly over the complete rational shooting bracket.
10. Exact defect/Jacobian recomputation validates a conditional two-cell
    normalized-polynomial Taylor track, but not yet wall continuation.
11. A validated partial-track audit exposes severe interval wrapping, while a
    global self-adjoint BVP probe finds a simple rational coercivity witness;
    the latter is now the selected AU.1 route.
12. Differentiating the uniform origin contraction certifies the exact cutoff
    boundary sensitivities needed by the augmented BVP Schur complement.
13. A globally `C2` rational BVP proposal and trusted conditional Barta checker
    now validate the certificate format and coercivity formulas. Adaptive exact
    subdivision proves the fixed witness globally on the complete approximate
    spline with a lower bound above `3/2`; centered residual bounds still do not
    close Newton.
14. The augmented proof now has an exact scalar Schur factorization and
    Newton-radii design in a graph norm controlling `C1`; exact second origin
    derivatives are certified, and a distinct wall-Dirichlet auxiliary candidate
    has exact rational wall zero. A trusted conditional audit now applies its
    symbolic left lift and rejects the broad residual correction. The residual
    evaluator now preserves the exact five-harmonic polynomial cancellation,
    and an exact graded mesh removes the genuine coarse first-cell defect at the
    floating level. The exact 43-cell graded representer now passes the trusted
    same-operator audit, proving
    `C_tau<=9.895351050897547` after a Barta lower bound greater than
    `1.0235900944571767` and exact sign-aware `L1` integration. A proof-ordered
    trace-sharpened Schur checker then certifies the corrected interval
    `[2.9014354731562104,3.017938531159798]` for the physical graded auxiliary
    of the exact approximate-profile operator. The endpoint-corrected nonlinear
    residual audit further proves `||G(F_bar)||_infinity<=0.002295967024672295`
    and cutoff-slope residual in
    `[-2.3462001836132805e-5,1.633679528688307e-5]`.
15. The row-local Green, center-equation Hessian, and weighted-trace audit closes
    the nonlinear tube at `omega=3/4`, `r=1/250`: conservatively,
    `Z0<=0.000078`, `Z2<=78.733`, `p(r)<-0.00107`, and `q(r)<0.31501`.
    It also certifies strict monotonicity, negative wall slope, and finite
    positive inertia. AU.1 is therefore complete for the prescribed
    fixed-background hard-wall problem.

## Required Gates

1. **AU.1, complete:** one augmented global boundary-value solution is enclosed
   by a rational spline residual, Barta coercivity inequality, nonzero scalar
   Schur complement, and closed Newton radius, with certified monotonicity,
   wall slope, and inertia at the default parameter point.
2. **AU.2, complete:** exact rational origin and 43-cell positive-radius bounds
   enclose the six third-derivative `L1` norms and evaluate the continuum tail
   envelope. Archive SHA256:
   `1d5fe53786cc280006d7b1092d360556d4d8d8684e5ae3356ce8cd6d084e72a9`.
3. **AU.3a, complete:** directed rational kernel bounds on `0<=p<=128`, joined
   to the AU.2 tail at the same frequency, certify conservative global
   `Q_0,Q_1,Q_2,G,M_1`. **AU.3b:** replace the profile-uniform finite-band
   envelope by sharp interval evaluation of the certified profile.
4. Derive the amplitude switch and collective-current coupling from one finite
   worldtube action, including a quantitative band-projection remainder.
5. Prove on a certified open parameter box or controlled scaling family that
   the lifetime exceeds the required preparation age plus heat/readout time,
   while compactness, peak local stress, junction residual, the lowest stability
   eigenvalue, and the support-region metric perturbation retain stated margins.
6. Compare the final theorem with Skyrmion cavity, detector/GKSL, ULE, and
   finite-QRF literature and pass an external novelty kill test.

## Go Or Kill Record

One parameter record is a consistency check. Proceed to submission only if an
open certified parameter box or controlled scaling family simultaneously has:

```text
rigorous ULE residual <= declared budget,
projection and switching remainders <= half the remaining budget,
lifetime >= B+T_chi+T_heat+T_readout,
2GM/a < declared subcritical margin,
peak local stress and junction residual < declared budgets,
lowest stability eigenvalue > declared positive margin,
support-region metric perturbation < declared weak-backreaction budget,
nonzero observable curvature or deformation discriminator.
```

Reframe as a no-go if every controlled parameter family violates the lifetime,
stress, or preparation inequality. Stop the novelty claim if the final theorem
is a direct corollary of existing ULE or detector results after substituting the
form factor.

## Proposed Paper Structure

1. Supported Skyrmion worldtube and collective current.
2. Optical static-patch spectrum and zero-mode prediction.
3. Principal-root obstruction and signed-factor lemma.
4. Sharp continuum tail and validated global constants.
5. Finite preparation, protocol window, and lifetime/stress budget.
6. Translation no-go and deformation observable.
7. Scope, comparison, and escape routes.
